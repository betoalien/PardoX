<?php

namespace PardoX\DataFrameOps;

trait ClusterTrait
{
    // Cluster operations are primarily exposed as IO static methods.
    // See IO::clusterScatter(), IO::clusterSql(), etc.
    // This trait is reserved for any future instance-level cluster methods.
}
