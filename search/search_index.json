{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Welcome to PardoX","text":"<p>The Speed of Rust. The Simplicity of Python.</p> <p>PardoX is a high-performance DataFrame engine designed for modern data engineering. It combines the safety and speed of a Rust Core with the ease of use of Python, PHP, and Node.js SDKs \u2014 allowing you to process massive datasets and integrate with any database without learning a new language.</p>"},{"location":"#why-pardox","title":"\ud83d\ude80 Why PardoX?","text":"<ul> <li>Zero-Copy Architecture: Data flows directly from disk or database into memory-mapped Rust buffers \u2014 no Python objects, no intermediate copies.</li> <li>SIMD Acceleration: Mathematical operations use AVX2/NEON CPU instructions for 5x\u201320x speedups vs. Python loops.</li> <li>Universal Compatibility: Runs natively on Windows, Linux, and macOS (Intel &amp; Apple Silicon).</li> <li>Native Database Engine: Connect to PostgreSQL, MySQL, SQL Server, and MongoDB entirely through Rust \u2014 no Python database drivers required.</li> <li>Multi-SDK: A single Rust core powers identical APIs in Python, Node.js, and PHP.</li> <li>Native Format: The <code>.prdx</code> binary format enables ~4.6 GB/s read throughput for repeated workloads.</li> </ul>"},{"location":"#documentation","title":"\ud83d\udcda Documentation","text":""},{"location":"#getting-started","title":"\ud83c\udfc1 Getting Started","text":"<ul> <li>Installation \u2014 Setup guide for Python, Node.js, and PHP.</li> <li>Quick Start \u2014 Build your first ETL pipeline in 5 minutes.</li> <li>Roadmap \u2014 What's coming in v0.3.2 and beyond.</li> </ul>"},{"location":"#user-guide","title":"\ud83d\udcd8 User Guide","text":"<ul> <li>Input / Output \u2014 Multi-threaded CSV, native .prdx format, Apache Arrow bridge.</li> <li>Databases \u2014 PostgreSQL, MySQL, SQL Server, MongoDB \u2014 read, write, and execute.</li> <li>Data Mutation \u2014 Vectorized arithmetic, type casting, sorting, data cleaning.</li> <li>Aggregations &amp; Observer \u2014 Metrics, statistics, value counts, and full-DataFrame export.</li> <li>GPU Acceleration \u2014 GPU Bitonic sort and CPU fallback.</li> <li>ML Integration \u2014 Zero-copy NumPy bridge and Scikit-Learn compatibility.</li> </ul>"},{"location":"#api-reference","title":"\u2699\ufe0f API Reference","text":"<ul> <li>Full Reference \u2014 Detailed documentation of all classes, functions, and methods.</li> </ul>"},{"location":"#examples-notebooks","title":"\ud83d\udcd3 Examples &amp; Notebooks","text":"<ul> <li>Jupyter Notebooks \u2014 Interactive examples and real-world ETL scenarios.</li> <li>Benchmark Scripts \u2014 Processing 640 million rows and transforming data to <code>.prdx</code> format.</li> </ul>"},{"location":"#quick-install","title":"\ud83d\udce6 Quick Install","text":"<pre><code>pip install pardox\n</code></pre>"},{"location":"#whats-new-in-v031","title":"What's New in v0.3.1","text":"Pillar What was added Relational Conqueror Native read/write/execute for PostgreSQL, MySQL, SQL Server, MongoDB via Rust drivers The Observer <code>to_dict()</code>, <code>to_json()</code>, <code>value_counts()</code>, <code>unique()</code> \u2014 full-DataFrame export with proper heap memory management Native Math <code>df.add()</code>, <code>df.sub()</code>, <code>df.std()</code>, <code>df.min_max_scale()</code>, <code>df.sort_values()</code> \u2014 pure Rust arithmetic GPU Awakening <code>sort_values(gpu=True)</code> \u2014 WebGPU Bitonic sort with automatic CPU fallback ML Integration Zero-copy NumPy bridge via <code>__array__</code> protocol \u2014 direct pointer into Rust buffer PHP &amp; Node.js SDKs Full parity with Python SDK across all new features <p>Open Source Project distributed under the MIT License.</p> <p>More info: www.pardox.io</p>"},{"location":"reference/","title":"API Reference","text":"<p>Complete documentation of all classes, functions, and methods in the PardoX Python SDK v0.3.1.</p>"},{"location":"reference/#top-level-functions-import-pardox-as-px","title":"Top-Level Functions (<code>import pardox as px</code>)","text":""},{"location":"reference/#read_csv","title":"<code>read_csv</code>","text":"<p>Reads a CSV file into a DataFrame using the multi-threaded Rust parser.</p> <pre><code>def read_csv(path: str, schema: dict | None = None) -&gt; DataFrame\n</code></pre> Parameter Type Description <code>path</code> <code>str</code> Path to the <code>.csv</code> file. <code>schema</code> <code>dict</code> or <code>None</code> Optional column type overrides: <code>{\"col\": \"Float64\", ...}</code>. Supported types: <code>Int64</code>, <code>Float64</code>, <code>Utf8</code>. <p>Returns: <code>DataFrame</code></p> <pre><code>df = px.read_csv(\"sales.csv\")\ndf = px.read_csv(\"sales.csv\", schema={\"price\": \"Float64\", \"id\": \"Int64\"})\n</code></pre>"},{"location":"reference/#read_prdx","title":"<code>read_prdx</code>","text":"<p>Loads a native PardoX binary file (<code>.prdx</code>).</p> <pre><code>def read_prdx(path: str) -&gt; list[dict]\n</code></pre> Parameter Type Description <code>path</code> <code>str</code> Path to the <code>.prdx</code> file. <p>Returns: <code>list[dict]</code> (preview rows)</p>"},{"location":"reference/#from_arrow","title":"<code>from_arrow</code>","text":"<p>Zero-copy conversion from a PyArrow Table or RecordBatch.</p> <pre><code>def from_arrow(data: pyarrow.Table | pyarrow.RecordBatch) -&gt; DataFrame\n</code></pre> <pre><code>import pyarrow as pa, pardox as px\narrow_table = pa.Table.from_pydict({\"a\": [1, 2, 3]})\ndf = px.from_arrow(arrow_table)\n</code></pre>"},{"location":"reference/#pardoxio-database-io","title":"<code>pardox.io</code> \u2014 Database I/O","text":"<p>All database functions bypass the Python runtime \u2014 connection and data transfer happen entirely in the Rust core.</p>"},{"location":"reference/#postgresql","title":"PostgreSQL","text":""},{"location":"reference/#read_sqlconnection_string-query-dataframe","title":"<code>read_sql(connection_string, query) \u2192 DataFrame</code>","text":"<pre><code>from pardox.io import read_sql\ndf = read_sql(\"postgresql://user:pass@localhost:5432/db\", \"SELECT * FROM orders\")\n</code></pre>"},{"location":"reference/#execute_sqlconnection_string-query-int","title":"<code>execute_sql(connection_string, query) \u2192 int</code>","text":"<p>Executes DDL or DML. Returns rows affected (0 for DDL).</p> <pre><code>from pardox.io import execute_sql\nexecute_sql(CONN, \"DROP TABLE IF EXISTS orders\")\nexecute_sql(CONN, \"CREATE TABLE orders (id BIGINT, amount FLOAT)\")\nn = execute_sql(CONN, \"DELETE FROM orders WHERE status = 'cancelled'\")\n</code></pre> <p>Raises: <code>RuntimeError</code> on connection or SQL failure.</p>"},{"location":"reference/#mysql","title":"MySQL","text":""},{"location":"reference/#read_mysqlconnection_string-query-dataframe","title":"<code>read_mysql(connection_string, query) \u2192 DataFrame</code>","text":"<pre><code>from pardox.io import read_mysql\ndf = read_mysql(\"mysql://user:pass@localhost:3306/db\", \"SELECT * FROM products\")\n</code></pre>"},{"location":"reference/#execute_mysqlconnection_string-query-int","title":"<code>execute_mysql(connection_string, query) \u2192 int</code>","text":"<pre><code>from pardox.io import execute_mysql\nexecute_mysql(CONN, \"CREATE TABLE IF NOT EXISTS products (id BIGINT, price DOUBLE)\")\n</code></pre>"},{"location":"reference/#sql-server","title":"SQL Server","text":""},{"location":"reference/#read_sqlserverconnection_string-query-dataframe","title":"<code>read_sqlserver(connection_string, query) \u2192 DataFrame</code>","text":"<pre><code>from pardox.io import read_sqlserver\nCONN = \"Server=localhost,1433;Database=mydb;UID=sa;PWD=MyPwd;TrustServerCertificate=Yes\"\ndf = read_sqlserver(CONN, \"SELECT TOP 1000 * FROM dbo.orders\")\n</code></pre>"},{"location":"reference/#execute_sqlserverconnection_string-query-int","title":"<code>execute_sqlserver(connection_string, query) \u2192 int</code>","text":"<pre><code>from pardox.io import execute_sqlserver\nexecute_sqlserver(CONN, \"DROP TABLE IF EXISTS dbo.orders_bak\")\n</code></pre> <p>Password special characters</p> <p>Avoid <code>!</code> in SQL Server passwords. Known tiberius v0.12 bug \u2014 fix planned for v0.3.2.</p>"},{"location":"reference/#mongodb","title":"MongoDB","text":""},{"location":"reference/#read_mongodbconnection_string-db_dot_collection-dataframe","title":"<code>read_mongodb(connection_string, db_dot_collection) \u2192 DataFrame</code>","text":"<pre><code>from pardox.io import read_mongodb\ndf = read_mongodb(\"mongodb://admin:pass@localhost:27017\", \"mydb.orders\")\n</code></pre>"},{"location":"reference/#execute_mongodbconnection_string-database-command_json-int","title":"<code>execute_mongodb(connection_string, database, command_json) \u2192 int</code>","text":"<pre><code>from pardox.io import execute_mongodb\nexecute_mongodb(\"mongodb://...\", \"mydb\", '{\"drop\": \"orders_archive\"}')\n</code></pre>"},{"location":"reference/#class-dataframe","title":"Class: <code>DataFrame</code>","text":"<p>The main data structure. Holds an opaque pointer to a Rust <code>HyperBlockManager</code>.</p>"},{"location":"reference/#construction","title":"Construction","text":"<pre><code># From CSV\ndf = px.read_csv(\"file.csv\")\n\n# From SQL\ndf = read_sql(conn, \"SELECT \u2026\")\n\n# From MySQL / SQL Server / MongoDB\ndf = read_mysql(conn, query)\ndf = read_sqlserver(conn, query)\ndf = read_mongodb(conn, \"db.collection\")\n\n# From Arrow\ndf = px.from_arrow(arrow_table)\n</code></pre>"},{"location":"reference/#properties","title":"Properties","text":""},{"location":"reference/#shape-tupleint-int","title":"<code>shape \u2192 tuple[int, int]</code>","text":"<pre><code>rows, cols = df.shape\nprint(f\"{rows:,} rows \u00d7 {cols} columns\")\n</code></pre>"},{"location":"reference/#columns-liststr","title":"<code>columns \u2192 list[str]</code>","text":"<pre><code>print(df.columns)   # ['id', 'price', 'quantity', ...]\n</code></pre>"},{"location":"reference/#dtypes-dictstr-str","title":"<code>dtypes \u2192 dict[str, str]</code>","text":"<pre><code>print(df.dtypes)    # {'id': 'Utf8', 'price': 'Float64', 'quantity': 'Int64'}\n</code></pre>"},{"location":"reference/#inspection","title":"Inspection","text":""},{"location":"reference/#shown10","title":"<code>show(n=10)</code>","text":"<p>Prints the first <code>n</code> rows as an ASCII table to stdout.</p> <pre><code>df.show(5)\n</code></pre>"},{"location":"reference/#headn5-dataframe","title":"<code>head(n=5) \u2192 DataFrame</code>","text":"<p>Returns a new DataFrame with the first <code>n</code> rows.</p> <pre><code>top5 = df.head(5)\n</code></pre>"},{"location":"reference/#tailn5-dataframe","title":"<code>tail(n=5) \u2192 DataFrame</code>","text":"<p>Returns a new DataFrame with the last <code>n</code> rows.</p> <pre><code>last5 = df.tail(5)\n</code></pre>"},{"location":"reference/#ilocstart-end-dataframe","title":"<code>iloc(start, end) \u2192 DataFrame</code>","text":"<p>Returns rows in the range <code>[start, end)</code>.</p> <pre><code>subset = df.iloc(100, 200)   # rows 100\u2013199\n</code></pre>"},{"location":"reference/#type-operations","title":"Type Operations","text":""},{"location":"reference/#castcol-target_type-dataframe","title":"<code>cast(col, target_type) \u2192 DataFrame</code>","text":"<p>Converts a column to a new type in-place. Returns <code>self</code>.</p> <pre><code>df.cast(\"quantity\", \"Float64\")\ndf.cast(\"id\",       \"Utf8\")\n</code></pre> <p>Supported types: <code>Int64</code>, <code>Float64</code>, <code>Utf8</code></p>"},{"location":"reference/#arithmetic-methods","title":"Arithmetic Methods","text":"<p>All arithmetic methods return a new DataFrame with the result stored in a named column.</p>"},{"location":"reference/#mulcol_a-col_b-dataframe","title":"<code>mul(col_a, col_b) \u2192 DataFrame</code>","text":"<pre><code>revenue_df = df.mul(\"price\", \"quantity\")   # result column: 'result_mul'\n</code></pre>"},{"location":"reference/#addcol_a-col_b-dataframe","title":"<code>add(col_a, col_b) \u2192 DataFrame</code>","text":"<pre><code>total_df = df.add(\"price\", \"tax\")          # result column: 'result_add'\n</code></pre>"},{"location":"reference/#subcol_a-col_b-dataframe","title":"<code>sub(col_a, col_b) \u2192 DataFrame</code>","text":"<pre><code>profit_df = df.sub(\"revenue\", \"cost\")      # result column: 'result_sub'\n</code></pre>"},{"location":"reference/#stdcol-float","title":"<code>std(col) \u2192 float</code>","text":"<p>Sample standard deviation of a column. Pure Rust, no NumPy.</p> <pre><code>std_val = revenue_df.std(\"result_mul\")\n</code></pre>"},{"location":"reference/#min_max_scalecol-dataframe","title":"<code>min_max_scale(col) \u2192 DataFrame</code>","text":"<p>Normalizes column values to <code>[0, 1]</code>. Returns new DataFrame with <code>result_minmax</code>.</p> <pre><code>normed_df = df.min_max_scale(\"price\")\n</code></pre>"},{"location":"reference/#sorting","title":"Sorting","text":""},{"location":"reference/#sort_valuesby-ascendingtrue-gpufalse-dataframe","title":"<code>sort_values(by, ascending=True, gpu=False) \u2192 DataFrame</code>","text":"<p>Sorts the DataFrame by a <code>Float64</code> column. Returns a new sorted DataFrame.</p> <pre><code>sorted_df = df.sort_values(\"price\", ascending=True)\nsorted_df = df.sort_values(\"price\", ascending=False, gpu=True)\n</code></pre> Parameter Type Description <code>by</code> <code>str</code> Column name to sort by. Must be <code>Float64</code>. <code>ascending</code> <code>bool</code> <code>True</code> = ascending (default). <code>gpu</code> <code>bool</code> Use GPU Bitonic sort. Falls back to CPU if GPU unavailable."},{"location":"reference/#filtering","title":"Filtering","text":""},{"location":"reference/#filtermask-series-dataframe","title":"<code>filter(mask: Series) \u2192 DataFrame</code>","text":"<p>Applies a boolean Series as a row filter. Returns a new DataFrame.</p> <pre><code>mask   = df['price'] &gt; 100.0\nresult = df.filter(mask)\n</code></pre>"},{"location":"reference/#data-cleaning","title":"Data Cleaning","text":""},{"location":"reference/#fillnavalue-float-dataframe","title":"<code>fillna(value: float) \u2192 DataFrame</code>","text":"<p>Fills <code>NaN</code> / null values in all numeric columns in-place.</p> <pre><code>df.fillna(0.0)\n</code></pre>"},{"location":"reference/#rounddecimals-int-dataframe","title":"<code>round(decimals: int) \u2192 DataFrame</code>","text":"<p>Rounds all numeric columns in-place.</p> <pre><code>df.round(2)\n</code></pre>"},{"location":"reference/#observer-export-inspection","title":"Observer \u2014 Export &amp; Inspection","text":""},{"location":"reference/#to_dict-listdict","title":"<code>to_dict() \u2192 list[dict]</code>","text":"<p>Returns all rows as a list of dictionaries (records format).</p> <pre><code>records = df.to_dict()\n# [{'price': 19.99, 'state': 'TX', ...}, ...]\n</code></pre> <p>Returns: <code>list[dict]</code></p>"},{"location":"reference/#to_json-str","title":"<code>to_json() \u2192 str</code>","text":"<p>Returns all rows as a JSON string <code>\"[{...}, ...]\"</code>.</p> <pre><code>json_str = df.to_json()\n</code></pre> <p>Returns: <code>str</code></p>"},{"location":"reference/#value_countscol-dictstr-int","title":"<code>value_counts(col) \u2192 dict[str, int]</code>","text":"<p>Frequency of each unique value in a column, sorted by count descending.</p> <pre><code>state_dist = df.value_counts(\"state\")\n# {'TX': 6345, 'CA': 6301, ...}\n</code></pre> <p>Returns: <code>dict[str, int]</code></p>"},{"location":"reference/#uniquecol-list","title":"<code>unique(col) \u2192 list</code>","text":"<p>Unique values in a column in insertion order.</p> <pre><code>cats = df.unique(\"category\")\n# ['Electronics', 'Books', ...]\n</code></pre> <p>Returns: <code>list</code></p>"},{"location":"reference/#joins","title":"Joins","text":""},{"location":"reference/#joinother-onnone-left_onnone-right_onnone-dataframe","title":"<code>join(other, on=None, left_on=None, right_on=None) \u2192 DataFrame</code>","text":"<p>Hash-join two DataFrames on a key column.</p> <pre><code>result = orders.join(customers, on=\"customer_id\")\nresult = orders.join(customers, left_on=\"cust_id\", right_on=\"id\")\n</code></pre>"},{"location":"reference/#writers","title":"Writers","text":""},{"location":"reference/#to_prdxpath-bool","title":"<code>to_prdx(path) \u2192 bool</code>","text":"<p>Saves DataFrame to native binary format.</p> <pre><code>df.to_prdx(\"output.prdx\")\n</code></pre>"},{"location":"reference/#to_csvpath-bool","title":"<code>to_csv(path) \u2192 bool</code>","text":"<p>Exports DataFrame to a CSV file.</p> <pre><code>df.to_csv(\"output.csv\")\n</code></pre>"},{"location":"reference/#to_sqlconnection_string-table_name-modeappend-conflict_cols-int","title":"<code>to_sql(connection_string, table_name, mode=\"append\", conflict_cols=[]) \u2192 int</code>","text":"<p>Writes to PostgreSQL.</p> <pre><code>rows = df.to_sql(CONN, \"orders\", mode=\"append\")\nrows = df.to_sql(CONN, \"orders\", mode=\"upsert\", conflict_cols=[\"id\"])\n</code></pre> Parameter Type Values <code>mode</code> <code>str</code> <code>\"append\"</code>, <code>\"upsert\"</code> <code>conflict_cols</code> <code>list[str]</code> Columns for <code>ON CONFLICT</code> clause (upsert only) <p>Returns: <code>int</code> \u2014 rows written. Raises: <code>RuntimeError</code> on failure.</p>"},{"location":"reference/#to_mysqlconnection_string-table_name-modeappend-conflict_cols-int","title":"<code>to_mysql(connection_string, table_name, mode=\"append\", conflict_cols=[]) \u2192 int</code>","text":"<p>Writes to MySQL.</p> <pre><code>rows = df.to_mysql(CONN, \"products\", mode=\"append\")\nrows = df.to_mysql(CONN, \"products\", mode=\"replace\")\nrows = df.to_mysql(CONN, \"products\", mode=\"upsert\", conflict_cols=[\"id\"])\n</code></pre> Parameter Type Values <code>mode</code> <code>str</code> <code>\"append\"</code>, <code>\"replace\"</code>, <code>\"upsert\"</code>"},{"location":"reference/#to_sqlserverconnection_string-table_name-modeappend-conflict_cols-int","title":"<code>to_sqlserver(connection_string, table_name, mode=\"append\", conflict_cols=[]) \u2192 int</code>","text":"<p>Writes to SQL Server (batch INSERT 500 rows/stmt).</p> <pre><code>rows = df.to_sqlserver(CONN, \"dbo.orders\", mode=\"append\")\nrows = df.to_sqlserver(CONN, \"dbo.orders\", mode=\"upsert\", conflict_cols=[\"id\"])\n</code></pre> Parameter Type Values <code>mode</code> <code>str</code> <code>\"append\"</code>, <code>\"replace\"</code>, <code>\"upsert\"</code>"},{"location":"reference/#to_mongodbconnection_string-db_dot_collection-modeappend-int","title":"<code>to_mongodb(connection_string, db_dot_collection, mode=\"append\") \u2192 int</code>","text":"<p>Writes to MongoDB (10,000 docs/batch, <code>ordered: false</code>).</p> <pre><code>rows = df.to_mongodb(CONN, \"mydb.orders\", mode=\"append\")\nrows = df.to_mongodb(CONN, \"mydb.orders\", mode=\"replace\")\n</code></pre> Parameter Type Values <code>mode</code> <code>str</code> <code>\"append\"</code>, <code>\"replace\"</code>"},{"location":"reference/#class-series","title":"Class: <code>Series</code>","text":"<p>A single column view into a DataFrame. Returned by <code>df['col_name']</code>. Does not own the underlying memory \u2014 the parent DataFrame does.</p>"},{"location":"reference/#properties_1","title":"Properties","text":""},{"location":"reference/#name-str","title":"<code>name \u2192 str</code>","text":"<p>Column name.</p>"},{"location":"reference/#dtype-str","title":"<code>dtype \u2192 str</code>","text":"<p>Column type (<code>\"Int64\"</code>, <code>\"Float64\"</code>, <code>\"Utf8\"</code>).</p>"},{"location":"reference/#arithmetic-operators","title":"Arithmetic Operators","text":"<p>Operations dispatch to SIMD-accelerated Rust kernels. All return a new <code>Series</code>.</p> <pre><code>total = df['price'] * df['quantity']\nnet   = df['total'] - df['discount']\ntax   = df['total'] + df['tax_amount']\nunit  = df['revenue'] / df['quantity']\n</code></pre>"},{"location":"reference/#comparison-operators","title":"Comparison Operators","text":"<p>Return a boolean <code>Series</code> usable as a filter mask.</p> Method Meaning <code>s.eq(val)</code> <code>==</code> <code>s.neq(val)</code> <code>!=</code> <code>s.gt(val)</code> <code>&gt;</code> <code>s.gte(val)</code> <code>&gt;=</code> <code>s.lt(val)</code> <code>&lt;</code> <code>s.lte(val)</code> <code>&lt;=</code> <pre><code>mask = df['price'].gt(100.0)\ndf_filtered = df.filter(mask)\n\nmask2 = df['state'].eq(\"TX\")\ndf_tx = df.filter(mask2)\n</code></pre>"},{"location":"reference/#aggregations","title":"Aggregations","text":"<p>All aggregation methods return a Python scalar.</p> Method Returns Description <code>sum()</code> <code>float</code> Sum of all non-null values <code>mean()</code> <code>float</code> Arithmetic mean <code>min()</code> <code>float</code> Minimum value <code>max()</code> <code>float</code> Maximum value <code>std()</code> <code>float</code> Sample standard deviation <code>count()</code> <code>int</code> Count of non-null values <pre><code>total   = df['revenue'].sum()\naverage = df['revenue'].mean()\nhigh    = df['revenue'].max()\nlow     = df['revenue'].min()\nspread  = df['revenue'].std()\nvalid   = df['id'].count()\n</code></pre>"},{"location":"reference/#transformations","title":"Transformations","text":""},{"location":"reference/#fillnavalue-series","title":"<code>fillna(value) \u2192 Series</code>","text":"<pre><code>df['price'].fillna(0.0)\n</code></pre>"},{"location":"reference/#rounddecimals-series","title":"<code>round(decimals) \u2192 Series</code>","text":"<pre><code>df['price'].round(2)\n</code></pre>"},{"location":"reference/#numpy-zero-copy","title":"NumPy Zero-Copy","text":"<pre><code>import numpy as np\n\n# Direct pointer into Rust buffer \u2014 no allocation\narr = np.array(df['price'])   # dtype: float64\n</code></pre> <p>Works on <code>Float64</code> columns. Cast <code>Int64</code> columns first:</p> <pre><code>df.cast(\"quantity\", \"Float64\")\narr = np.array(df[\"quantity\"])\n</code></pre>"},{"location":"reference/#error-codes","title":"Error Codes","text":"<p>All database functions raise <code>RuntimeError</code> with a descriptive message on failure. The underlying Rust function returns integer error codes:</p> Code Meaning <code>-1</code> Invalid manager pointer (null) <code>-2</code> Invalid connection string <code>-3</code> Invalid table / query string <code>-4</code> Invalid mode string <code>-5</code> Invalid conflict columns JSON <code>-100</code> Operation failed \u2014 check stderr for Rust error details <p>Stderr logging</p> <p>When <code>-100</code> is returned, the Rust core logs the actual database error to stderr before returning. Run with stderr visible to diagnose connection or schema issues.</p> <p>A hierarchical error code system (connection errors, SQL syntax errors, constraint violations, etc.) is planned for v0.3.2.</p>"},{"location":"guide/","title":"User Guide","text":"<p>Explore core workflows, data operations, and advanced acceleration features.</p> <ul> <li>Input / Output Operations</li> <li>Database Integration</li> <li>Data Mutation &amp; Arithmetic</li> <li>Aggregations, Analytics &amp; The Observer</li> <li>GPU Acceleration</li> <li>ML Integration \u2014 Zero-Copy NumPy Bridge</li> </ul>"},{"location":"guide/aggregations/","title":"Aggregations, Analytics &amp; The Observer","text":"<p>PardoX provides two layers of data insight:</p> <ol> <li>Aggregation kernels \u2014 reduce a column to a scalar (sum, mean, std, etc.)</li> <li>The Observer \u2014 export and inspect the full DataFrame (value_counts, unique, to_dict, to_json)</li> </ol> <p>All operations run entirely in native Rust machine code.</p>"},{"location":"guide/aggregations/#1-column-aggregations-series","title":"1. Column Aggregations (Series)","text":"<p>Access aggregation methods directly on any numeric <code>Series</code> object (obtained via <code>df['col']</code>).</p>"},{"location":"guide/aggregations/#sum","title":"<code>sum()</code>","text":"<pre><code>total_revenue = df['amount'].sum()\nprint(f\"Total: ${total_revenue:,.2f}\")\n</code></pre> <p>Returns: <code>float</code></p>"},{"location":"guide/aggregations/#mean","title":"<code>mean()</code>","text":"<pre><code>avg_ticket = df['amount'].mean()\n</code></pre> <p>Returns: <code>float</code></p>"},{"location":"guide/aggregations/#min-max","title":"<code>min()</code> / <code>max()</code>","text":"<pre><code>highest = df['amount'].max()\nlowest  = df['amount'].min()\n</code></pre> <p>Returns: <code>float</code></p>"},{"location":"guide/aggregations/#std","title":"<code>std()</code>","text":"<p>Sample standard deviation.</p> <pre><code>volatility = df['amount'].std()\n</code></pre> <p>Returns: <code>float</code></p> <p>Interpretation</p> <p>Low std \u2192 values cluster near the mean. High std \u2192 values are widely spread.</p>"},{"location":"guide/aggregations/#count","title":"<code>count()</code>","text":"<p>Number of non-null values in the column.</p> <pre><code>valid_transactions = df['transaction_id'].count()\n</code></pre> <p>Returns: <code>int</code></p>"},{"location":"guide/aggregations/#2-dataframe-level-standard-deviation","title":"2. DataFrame-Level Standard Deviation","text":"<p><code>df.std(col)</code> is a DataFrame method (distinct from the Series <code>.std()</code>) that computes the sample standard deviation for a named column and returns a Python float. Useful when working with derived DataFrames from <code>df.mul()</code>, <code>df.add()</code>, etc.</p> <pre><code>revenue_df = df.mul(\"price\", \"quantity\")\nstd_val    = revenue_df.std(\"result_mul\")\nprint(f\"Revenue std dev: {std_val:,.4f}\")\n</code></pre>"},{"location":"guide/aggregations/#3-null-value-handling","title":"3. Null Value Handling","text":"<p>All aggregation functions are null-aware: they skip <code>NaN</code> / null values and compute metrics only over valid data points. This matches SQL and Pandas behavior.</p> <p>To fill nulls before aggregation:</p> <pre><code>df.fillna(0.0)\ntotal = df['amount'].sum()\n</code></pre>"},{"location":"guide/aggregations/#4-performance","title":"4. Performance","text":"Method Mechanism Speed (10M rows) Python <code>sum(list)</code> Iterates <code>PyObject</code> one by one ~1.5s PardoX <code>.sum()</code> SIMD vectorized accumulator ~0.02s <p>Under the hood</p> <p><code>.sum()</code> passes the memory pointer directly to a Rust function. AVX2 instructions add 4\u20138 values per CPU cycle without ever materializing Python objects.</p>"},{"location":"guide/aggregations/#5-the-observer-full-dataframe-export","title":"5. The Observer \u2014 Full DataFrame Export","text":"<p>The Observer module provides functions to export or inspect the entire DataFrame. All string results are heap-allocated (proper ownership) and freed after the Python string is created.</p>"},{"location":"guide/aggregations/#to_dict","title":"<code>to_dict()</code>","text":"<p>Returns all rows as a list of dictionaries (records format). Equivalent to Pandas' <code>df.to_dict('records')</code>.</p> <pre><code>records = df.to_dict()\n# [{'price': 19.99, 'quantity': 3, ...}, ...]\n\nprint(f\"Total records: {len(records)}\")\nfirst_row = records[0]\nprint(first_row['price'])\n</code></pre> <p>Returns: <code>list[dict]</code></p>"},{"location":"guide/aggregations/#to_json","title":"<code>to_json()</code>","text":"<p>Returns all rows as a JSON string <code>\"[{...}, ...]\"</code>. Useful for API responses or file writing.</p> <pre><code>json_str = df.to_json()\nwith open(\"output.json\", \"w\") as f:\n    f.write(json_str)\n</code></pre> <p>Returns: <code>str</code></p>"},{"location":"guide/aggregations/#value_countscol","title":"<code>value_counts(col)</code>","text":"<p>Returns the frequency of each unique value in a column, sorted by count descending.</p> <pre><code>state_counts = df.value_counts(\"state\")\n# {'TX': 6345, 'CA': 6301, 'CO': 6304, ...}\n\nprint(f\"Unique states: {len(state_counts)}\")\n\n# Top 5\nfor state, count in list(state_counts.items())[:5]:\n    print(f\"  {state}: {count:,}\")\n</code></pre> <p>Returns: <code>dict[str, int]</code></p>"},{"location":"guide/aggregations/#uniquecol","title":"<code>unique(col)</code>","text":"<p>Returns the unique values of a column in insertion order.</p> <pre><code>categories = df.unique(\"category\")\n# ['Electronics', 'Books', 'Clothing', ...]\n\nprint(f\"Distinct categories: {len(categories)}\")\n</code></pre> <p>Returns: <code>list</code></p>"},{"location":"guide/aggregations/#6-observer-php-and-nodejs","title":"6. Observer \u2014 PHP and Node.js","text":""},{"location":"guide/aggregations/#nodejs","title":"Node.js","text":"<pre><code>// value_counts\nconst stateCounts = df.valueCounts('state');\nconsole.log(`States: ${Object.keys(stateCounts).length}`);\n\n// unique\nconst cats = df.unique('category');\n\n// Full export\nconst records = df.toDict();       // array of objects\nconst jsonStr = df.toJson();       // JSON string\n\n// tolist \u2014 array of arrays (values only)\nconst matrix = df.tolist();\n</code></pre>"},{"location":"guide/aggregations/#php","title":"PHP","text":"<pre><code>// value_counts\n$stateCounts = $df-&gt;value_counts('state');\necho count($stateCounts) . \" unique states\\n\";\n\n// unique\n$cats = $df-&gt;unique('category');\n\n// Full export\n$records = $df-&gt;to_dict();    // array of assoc arrays\n$json    = $df-&gt;to_json();    // JSON string\n\n// tolist \u2014 array of arrays\n$matrix  = $df-&gt;tolist();\n</code></pre>"},{"location":"guide/aggregations/#7-complete-analysis-pipeline","title":"7. Complete Analysis Pipeline","text":"<pre><code>import pardox as px\nfrom pardox.io import execute_sql\n\ndf = px.read_csv(\"sales_50k.csv\")\ndf.cast(\"quantity\", \"Float64\")\n\n# Feature engineering\nrevenue_df = df.mul(\"price\", \"quantity\")\n\n# Aggregations\nprint(f\"Total revenue : ${df['price'].sum():,.2f}\")\nprint(f\"Avg price     : ${df['price'].mean():,.4f}\")\nprint(f\"Max price     : ${df['price'].max():,.2f}\")\nprint(f\"Revenue std   : {revenue_df.std('result_mul'):,.2f}\")\nprint(f\"Valid rows    : {df['transaction_id'].count():,}\")\n\n# EDA inspection\nstate_counts  = df.value_counts(\"state\")\ncategories    = df.unique(\"category\")\nprint(f\"\\nTop 3 states: {list(state_counts.items())[:3]}\")\nprint(f\"Categories:   {categories}\")\n\n# Full export\nrecords = df.to_dict()\nprint(f\"\\nExported {len(records):,} records to Python list\")\n</code></pre>"},{"location":"guide/aggregations/#8-groupby-roadmap","title":"8. GroupBy (Roadmap)","text":"<p>Vectorized <code>groupby</code> with a Rust hash-aggregation engine is planned for v0.3.2.</p> <pre><code># Coming in v0.3.2\nsummary = df.groupby(\"region\").agg({\n    \"revenue\": \"sum\",\n    \"quantity\": \"mean\",\n    \"transaction_id\": \"count\"\n})\n</code></pre> <p>For now, use <code>value_counts</code> for frequency analysis and filter + aggregate on subsets:</p> <pre><code># Workaround: filter then aggregate\ntx_mask = df['state'].eq(\"TX\")\ntx_df   = df.filter(tx_mask)\nprint(f\"TX revenue: ${tx_df['amount'].sum():,.2f}\")\n</code></pre>"},{"location":"guide/databases/","title":"Database Integration","text":"<p>PardoX v0.3.1 introduces native database connectivity via Rust drivers \u2014 no Python, PHP, or Node.js database libraries required. The same Rust core powers identical APIs across all three SDKs.</p>"},{"location":"guide/databases/#architecture","title":"Architecture","text":"<pre><code>Python / Node.js / PHP\n        \u2502\n        \u2502  connection string + DataFrame pointer\n        \u25bc\n   Rust Core (libpardox.so)\n        \u2502\n        \u251c\u2500\u2500 tokio-postgres  \u2192 PostgreSQL\n        \u251c\u2500\u2500 mysql v25       \u2192 MySQL / MariaDB\n        \u251c\u2500\u2500 tiberius v0.12  \u2192 SQL Server\n        \u2514\u2500\u2500 mongodb v2.8    \u2192 MongoDB\n</code></pre> <p>The host language sends a connection string and a raw pointer to the HyperBlock. Rust manages the entire wire protocol, serialization, and bulk transfer. No ORM, no query builder, no intermediate Python objects.</p>"},{"location":"guide/databases/#supported-databases","title":"Supported Databases","text":"Database Rust driver Read Write Execute DDL/DML PostgreSQL <code>tokio-postgres</code> \u2705 \u2705 \u2705 MySQL / MariaDB <code>mysql</code> v25 \u2705 \u2705 \u2705 SQL Server <code>tiberius</code> v0.12 \u2705 \u2705 \u2705 MongoDB <code>mongodb</code> v2.8 \u2705 \u2705 \u2705"},{"location":"guide/databases/#connection-string-formats","title":"Connection String Formats","text":"Database Format PostgreSQL <code>postgresql://user:password@host:port/database</code> MySQL <code>mysql://user:password@host:port/database</code> SQL Server <code>Server=host,port;Database=db;UID=user;PWD=password;TrustServerCertificate=Yes</code> MongoDB <code>mongodb://user:password@host:port</code>"},{"location":"guide/databases/#postgresql","title":"PostgreSQL","text":""},{"location":"guide/databases/#python","title":"Python","text":"<pre><code>from pardox.io import read_sql, execute_sql\nimport pardox as px\n\nCONN = \"postgresql://pardox:secret@localhost:5432/mydb\"\n\n# Read\ndf = read_sql(CONN, \"SELECT * FROM orders WHERE status = 'active'\")\nprint(df.shape)   # (rows, cols)\n\n# DDL\nexecute_sql(CONN, \"DROP TABLE IF EXISTS orders_processed\")\nexecute_sql(CONN, \"\"\"\n    CREATE TABLE orders_processed (\n        id      BIGINT,\n        amount  DOUBLE PRECISION,\n        region  TEXT,\n        UNIQUE (id)\n    )\n\"\"\")\n\n# Write \u2014 append (auto COPY FROM STDIN for &gt; 10k rows)\nrows = df.to_sql(CONN, \"orders_processed\", mode=\"append\")\nprint(f\"Written {rows:,} rows\")\n\n# Write \u2014 upsert\nrows = df.to_sql(CONN, \"orders_processed\", mode=\"upsert\", conflict_cols=[\"id\"])\n</code></pre>"},{"location":"guide/databases/#nodejs","title":"Node.js","text":"<pre><code>const { read_sql, executeSql } = require('pardox/src/io');\n\nconst CONN = 'postgresql://pardox:secret@localhost:5432/mydb';\n\nconst df = read_sql(CONN, 'SELECT * FROM orders');\nexecuteSql(CONN, 'CREATE TABLE IF NOT EXISTS orders_bak (id BIGINT, amount FLOAT)');\n\nconst rows = df.toSql(CONN, 'orders_bak', 'append');\nconsole.log(`Written ${rows} rows`);\n\n// Upsert\nconst rows2 = df.toSql(CONN, 'orders_bak', 'upsert', ['id']);\n</code></pre>"},{"location":"guide/databases/#php","title":"PHP","text":"<pre><code>use PardoX\\DataFrame;\nuse PardoX\\IO;\n\n$CONN = 'postgresql://pardox:secret@localhost:5432/mydb';\n\n$df = IO::read_sql($CONN, 'SELECT * FROM orders');\nIO::executeSql($CONN, 'CREATE TABLE IF NOT EXISTS orders_bak (id BIGINT, amount FLOAT)');\n\n$rows = $df-&gt;to_sql($CONN, 'orders_bak', 'append');\necho \"Written $rows rows\\n\";\n\n// Upsert\n$rows = $df-&gt;to_sql($CONN, 'orders_bak', 'upsert', ['id']);\n</code></pre>"},{"location":"guide/databases/#write-modes","title":"Write modes","text":"Mode SQL generated <code>append</code> <code>INSERT INTO table (cols) VALUES \u2026</code> Auto-upgrades to <code>COPY FROM STDIN</code> for &gt; 10,000 rows <code>upsert</code> <code>INSERT INTO table (cols) VALUES \u2026 ON CONFLICT (key_cols) DO UPDATE SET \u2026</code>"},{"location":"guide/databases/#mysql","title":"MySQL","text":""},{"location":"guide/databases/#python_1","title":"Python","text":"<pre><code>from pardox.io import read_mysql, execute_mysql\n\nCONN = \"mysql://pardox:secret@localhost:3306/mydb\"\n\n# Read\ndf = read_mysql(CONN, \"SELECT * FROM products WHERE stock &gt; 0\")\n\n# DDL\nexecute_mysql(CONN, \"DROP TABLE IF EXISTS products_bak\")\nexecute_mysql(CONN, \"\"\"\n    CREATE TABLE products_bak (\n        id       BIGINT PRIMARY KEY,\n        name     TEXT,\n        price    DOUBLE,\n        quantity DOUBLE\n    )\n\"\"\")\n\n# Write\nrows = df.to_mysql(CONN, \"products_bak\", mode=\"append\")\nrows = df.to_mysql(CONN, \"products_bak\", mode=\"replace\")\nrows = df.to_mysql(CONN, \"products_bak\", mode=\"upsert\", conflict_cols=[\"id\"])\n</code></pre>"},{"location":"guide/databases/#nodejs_1","title":"Node.js","text":"<pre><code>const { read_mysql, execute_mysql } = require('pardox/src/io');\n\nconst df = read_mysql(CONN, 'SELECT * FROM products');\nexecute_mysql(CONN, 'CREATE TABLE IF NOT EXISTS products_bak (id BIGINT, price DOUBLE)');\n\nconst rows = df.toMysql(CONN, 'products_bak', 'append');\nconst rows2 = df.toMysql(CONN, 'products_bak', 'upsert', ['id']);\n</code></pre>"},{"location":"guide/databases/#php_1","title":"PHP","text":"<pre><code>$df = IO::read_mysql($CONN, 'SELECT * FROM products');\nIO::executeMysql($CONN, 'CREATE TABLE IF NOT EXISTS products_bak (id BIGINT, price DOUBLE)');\n\n$rows = $df-&gt;to_mysql($CONN, 'products_bak', 'append');\n$rows = $df-&gt;to_mysql($CONN, 'products_bak', 'upsert', ['id']);\n</code></pre>"},{"location":"guide/databases/#write-modes_1","title":"Write modes","text":"Mode SQL generated <code>append</code> <code>INSERT INTO table (cols) VALUES (chunk of 1,000 rows)</code> Auto-upgrades to <code>LOAD DATA LOCAL INFILE</code> for &gt; 10,000 rows if <code>local_infile=ON</code> <code>replace</code> <code>REPLACE INTO table (cols) VALUES (chunk of 1,000 rows)</code> <code>upsert</code> <code>INSERT INTO table (cols) VALUES \u2026 ON DUPLICATE KEY UPDATE col=VALUES(col)\u2026</code> <p>Enabling LOAD DATA LOCAL INFILE</p> <p><pre><code>SET GLOBAL local_infile = 1;\n-- or in my.cnf: local_infile=1\n</code></pre> If disabled, PardoX automatically falls back to chunked INSERT and logs a notice to stderr.</p>"},{"location":"guide/databases/#sql-server","title":"SQL Server","text":""},{"location":"guide/databases/#python_2","title":"Python","text":"<pre><code>from pardox.io import read_sqlserver, execute_sqlserver\n\nCONN = \"Server=localhost,1433;Database=mydb;UID=sa;PWD=MyPassword;TrustServerCertificate=Yes\"\n\n# Read\ndf = read_sqlserver(CONN, \"SELECT TOP 5000 * FROM dbo.transactions\")\n\n# DDL\nexecute_sqlserver(CONN, \"DROP TABLE IF EXISTS dbo.transactions_bak\")\nexecute_sqlserver(CONN, \"\"\"\n    CREATE TABLE dbo.transactions_bak (\n        id     BIGINT,\n        amount FLOAT,\n        region NVARCHAR(MAX)\n    )\n\"\"\")\n\n# Write \u2014 batch INSERT 500 rows per statement\nrows = df.to_sqlserver(CONN, \"dbo.transactions_bak\", mode=\"append\")\n\n# Upsert \u2014 MERGE INTO \u2026 WHEN MATCHED \u2026 WHEN NOT MATCHED\nrows = df.to_sqlserver(CONN, \"dbo.transactions_bak\", mode=\"upsert\", conflict_cols=[\"id\"])\n</code></pre>"},{"location":"guide/databases/#nodejs_2","title":"Node.js","text":"<pre><code>const { read_sqlserver, execute_sqlserver } = require('pardox/src/io');\n\nconst df = read_sqlserver(CONN, 'SELECT TOP 1000 * FROM dbo.orders');\nexecute_sqlserver(CONN, 'DROP TABLE IF EXISTS dbo.orders_bak');\n\nconst rows = df.toSqlserver(CONN, 'dbo.orders_bak', 'append');\nconst rows2 = df.toSqlserver(CONN, 'dbo.orders_bak', 'upsert', ['id']);\n</code></pre>"},{"location":"guide/databases/#php_2","title":"PHP","text":"<pre><code>$df = IO::read_sqlserver($CONN, 'SELECT TOP 1000 * FROM dbo.orders');\nIO::executeSqlserver($CONN, 'DROP TABLE IF EXISTS dbo.orders_bak');\n\n$rows = $df-&gt;to_sqlserver($CONN, 'dbo.orders_bak', 'append');\n$rows = $df-&gt;to_sqlserver($CONN, 'dbo.orders_bak', 'upsert', ['id']);\n</code></pre>"},{"location":"guide/databases/#write-modes_2","title":"Write modes","text":"Mode SQL generated <code>append</code> <code>INSERT INTO table (cols) VALUES (r1),(r2),\u2026,(r500)</code> \u2014 500 rows per statement <code>replace</code> Same multi-row INSERT <code>upsert</code> <code>MERGE INTO table AS T USING (VALUES \u2026) AS S (cols) ON (key) WHEN MATCHED \u2026 WHEN NOT MATCHED \u2026</code> <p>Password special characters</p> <p>A known issue in tiberius v0.12 causes authentication failure when the SQL Server password contains <code>!</code>. Use passwords with only <code>[A-Za-z0-9_\\-@#$]</code>. A fix is tracked for v0.3.2.</p>"},{"location":"guide/databases/#mongodb","title":"MongoDB","text":""},{"location":"guide/databases/#python_3","title":"Python","text":"<pre><code>from pardox.io import read_mongodb, execute_mongodb\n\nCONN = \"mongodb://admin:secret@localhost:27017\"\n\n# Read \u2014 \"database.collection\"\ndf = read_mongodb(CONN, \"mydb.orders\")\nprint(df.shape)\n\n# Command (admin operations)\nexecute_mongodb(CONN, \"mydb\", '{\"drop\": \"orders_archive\"}')\n\n# Write \u2014 append (insert_many 10k/batch, ordered:false)\nrows = df.to_mongodb(CONN, \"mydb.orders_archive\", mode=\"append\")\n\n# Write \u2014 replace (drop collection, then re-insert)\nrows = df.to_mongodb(CONN, \"mydb.orders_archive\", mode=\"replace\")\n</code></pre>"},{"location":"guide/databases/#nodejs_3","title":"Node.js","text":"<pre><code>const { read_mongodb, execute_mongodb } = require('pardox/src/io');\n\nconst df = read_mongodb(CONN, 'mydb.orders');\nexecute_mongodb(CONN, 'mydb', '{\"drop\": \"orders_archive\"}');\n\nconst rows = df.toMongodb(CONN, 'mydb.orders_archive', 'append');\n</code></pre>"},{"location":"guide/databases/#php_3","title":"PHP","text":"<pre><code>$df = IO::read_mongodb($CONN, 'mydb.orders');\nIO::executeMongodb($CONN, 'mydb', '{\"drop\": \"orders_archive\"}');\n\n$rows = $df-&gt;to_mongodb($CONN, 'mydb.orders_archive', 'append');\n</code></pre>"},{"location":"guide/databases/#write-modes_3","title":"Write modes","text":"Mode Behavior <code>append</code> <code>insert_many</code> in batches of 10,000 documents, <code>ordered: false</code> <code>replace</code> Drops the collection, then <code>insert_many</code> in batches of 10,000"},{"location":"guide/databases/#bulk-write-performance","title":"Bulk Write Performance","text":"Database Rows &lt; 10k Rows \u2265 10k (append) Protocol PostgreSQL Multi-row INSERT COPY FROM STDIN (auto) Wire-level streaming MySQL 1,000 rows/INSERT LOAD DATA LOCAL INFILE (auto, if <code>local_infile=ON</code>) In-memory CSV buffer SQL Server 500 rows/INSERT 500 rows/INSERT Multi-value INSERT MongoDB 10,000 docs/batch 10,000 docs/batch <code>insert_many</code>"},{"location":"guide/databases/#validated-results-v031","title":"Validated Results (v0.3.1)","text":"<p>All validations used a 50,000-row real-world dataset (<code>hardcore_sales_50k.csv</code>).</p> Database SDK Result PostgreSQL Python \u2705 50,000 rows \u2014 COPY FROM STDIN PostgreSQL Node.js \u2705 50,000 rows \u2014 COPY FROM STDIN MySQL Python \u2705 50,000 rows \u2014 chunked batch (LOAD DATA disabled server-side) MySQL PHP \u2705 50,000 rows \u2014 chunked batch SQL Server all \u26a0\ufe0f Authentication fails with <code>!</code> in password \u2014 fix in v0.3.2 MongoDB \u2014 Implemented; bulk benchmarks planned"},{"location":"guide/gpu/","title":"GPU Acceleration","text":"<p>PardoX v0.3.1 introduces a GPU Bitonic Sort pipeline powered by WebGPU compute shaders via the <code>wgpu</code> crate. The GPU engine is loaded dynamically from a separate library (<code>libpardox_gpu.so</code>), keeping it completely optional.</p>"},{"location":"guide/gpu/#how-it-works","title":"How It Works","text":"<pre><code>df.sort_values(\"col\", gpu=True)\n        \u2502\n        \u25bc\n   pardox_gpu_sort (Rust FFI)\n        \u2502\n        \u251c\u2500\u2500 Load libpardox_gpu.so dynamically (libloading 0.8)\n        \u251c\u2500\u2500 Extract Float64 column into contiguous buffer\n        \u251c\u2500\u2500 Transfer buffer to GPU (VRAM)\n        \u251c\u2500\u2500 Execute Bitonic Sort compute shader (WebGPU / wgpu 0.19)\n        \u251c\u2500\u2500 Read sorted buffer back from GPU\n        \u2514\u2500\u2500 Apply row permutation to full DataFrame\n        \u2502\n        \u25bc  (if GPU unavailable)\n   CPU fallback \u2014 parallel Rust merge sort (Rayon)\n</code></pre> <p>The GPU sort works exclusively on <code>Float64</code> columns. The result is applied back to the full DataFrame row order via an argsort + permutation step entirely in Rust.</p>"},{"location":"guide/gpu/#usage-python","title":"Usage \u2014 Python","text":"<pre><code># CPU sort (default)\nsorted_df = df.sort_values(\"revenue\", ascending=True)\nsorted_df = df.sort_values(\"revenue\", ascending=False)\n\n# GPU Bitonic sort\nsorted_df = df.sort_values(\"revenue\", ascending=True, gpu=True)\n\nprint(f\"Rows: {sorted_df.shape[0]:,}\")\n</code></pre>"},{"location":"guide/gpu/#usage-nodejs","title":"Usage \u2014 Node.js","text":"<pre><code>// CPU sort\nconst sortedDf = df.sortValues('revenue', true);\n\n// GPU sort\nconst sortedDf = df.sortValues('revenue', true, true);  // (by, ascending, gpu)\n</code></pre>"},{"location":"guide/gpu/#usage-php","title":"Usage \u2014 PHP","text":"<pre><code>// CPU sort\n$sorted = $df-&gt;sort_values('revenue', true);\n\n// GPU sort\n$sorted = $df-&gt;sort_values('revenue', true, true);  // ($by, $ascending, $gpu)\n</code></pre>"},{"location":"guide/gpu/#cpu-fallback","title":"CPU Fallback","text":"<p>If a compatible GPU is not available or the <code>wgpu</code> backend fails to initialize:</p> <ol> <li>PardoX logs <code>[PardoX GPU Sort] GPU not available, using CPU sort.</code> to stderr.</li> <li>The CPU parallel merge sort (Rayon) is used transparently.</li> <li>The result is identical \u2014 only the execution path differs.</li> </ol> <p>This means you can safely call <code>gpu=True</code> in all environments without adding error handling.</p>"},{"location":"guide/gpu/#gpu-requirements","title":"GPU Requirements","text":"Requirement Details API WebGPU via <code>wgpu</code> 0.19 Backends Vulkan (Linux / Windows), Metal (macOS), DX12 (Windows) VRAM Minimum ~256 MB for 50k rows (Float64 = 400 KB; permutation overhead ~4 MB) Adapter Any discrete or integrated GPU with Vulkan / Metal / DX12 support <p>WebGPU \u2260 browser WebGPU</p> <p>PardoX uses the native Rust <code>wgpu</code> crate, which targets Vulkan/Metal/DX12 directly. It does not require a browser or WebAssembly environment.</p>"},{"location":"guide/gpu/#dynamic-library-loading","title":"Dynamic Library Loading","text":"<p>The GPU engine is shipped as a separate shared library (<code>libpardox_gpu.so</code> / <code>.dylib</code> / <code>.dll</code>) loaded at runtime via <code>libloading 0.8</code>. The main <code>libpardox.so</code> will function normally even if <code>libpardox_gpu.so</code> is absent \u2014 GPU calls simply fall back to CPU.</p>"},{"location":"guide/gpu/#performance","title":"Performance","text":"<p>GPU sort shines for very large <code>Float64</code> columns. For small datasets (&lt; 100k rows), the overhead of GPU transfer and shader dispatch may outweigh the benefit.</p> Rows CPU sort GPU Bitonic sort 50k ~5ms ~15ms (transfer overhead dominates) 1M ~120ms ~40ms 10M ~1.4s ~280ms <p>When to use GPU</p> <p>Use <code>gpu=True</code> when sorting columns with more than 500,000 rows. For smaller datasets, the default CPU path is faster due to lower overhead.</p>"},{"location":"guide/io/","title":"Input / Output Operations","text":"<p>The bottleneck in most data pipelines is not computation, but IO. PardoX moves all data ingestion and persistence entirely into the Rust core, bypassing Python's file handling and object creation overhead.</p>"},{"location":"guide/io/#1-csv-files","title":"1. CSV Files","text":"<p>PardoX features a multi-threaded CSV reader. The file is memory-mapped and parallel workers parse chunks simultaneously. No Python objects are created during ingestion.</p>"},{"location":"guide/io/#basic-usage","title":"Basic usage","text":"<pre><code>import pardox as px\n\n# Automatic type inference (Int64, Float64, Utf8)\ndf = px.read_csv(\"dataset.csv\")\nprint(df.shape)    # (rows, cols)\nprint(df.columns)  # ['col1', 'col2', ...]\n</code></pre>"},{"location":"guide/io/#manual-schema","title":"Manual schema","text":"<pre><code>df = px.read_csv(\"dataset.csv\", schema={\n    \"price\":    \"Float64\",\n    \"quantity\": \"Float64\",\n    \"id\":       \"Int64\",\n    \"name\":     \"Utf8\",\n})\n</code></pre> <p>Intelligent type inference</p> <p>Without a schema, the engine scans the first rows to detect <code>Int64</code>, <code>Float64</code>, or <code>Utf8</code>. Pass a schema to override for specific columns.</p> <p>Parallel parsing</p> <p>The file is split into logical blocks processed by multiple CPU cores concurrently.</p>"},{"location":"guide/io/#2-native-format-prdx","title":"2. Native Format (<code>.prdx</code>)","text":"<p>The PRDX format is a custom binary layout designed for instant persistence. Reading is a near-direct memory copy \u2014 no parsing, no schema detection.</p>"},{"location":"guide/io/#save","title":"Save","text":"<pre><code>df.to_prdx(\"output.prdx\")\n</code></pre>"},{"location":"guide/io/#load","title":"Load","text":"<pre><code>df = px.read_prdx(\"output.prdx\")\n</code></pre> <p>Benchmark</p> <p>In tests with 10 GB datasets, reading a <code>.prdx</code> file achieves ~4.6 GB/s throughput, limited only by NVMe SSD speed.</p> Format Read speed (2 GB file) CSV ~8s Parquet ~3s PRDX ~0.5s"},{"location":"guide/io/#3-apache-arrow-bridge","title":"3. Apache Arrow Bridge","text":"<p>PardoX integrates with the Arrow ecosystem via the Arrow C Data Interface. Conversion passes memory pointers \u2014 no data is copied.</p> <pre><code>import pyarrow as pa\nimport pardox as px\n\narrow_table = pa.Table.from_pydict({\"price\": [1.5, 2.0], \"qty\": [10, 20]})\ndf = px.from_arrow(arrow_table)\n</code></pre> <p>Interoperability</p> <p>The Arrow bridge connects PardoX to Polars, DuckDB, Apache Spark, and any other tool that supports the Arrow IPC format.</p>"},{"location":"guide/io/#4-postgresql","title":"4. PostgreSQL","text":"<p>PardoX uses <code>tokio-postgres</code> inside the Rust core. The host language (Python / Node.js / PHP) never touches the database wire protocol.</p>"},{"location":"guide/io/#connection-string","title":"Connection string","text":"<pre><code>postgresql://user:password@host:port/database\n</code></pre>"},{"location":"guide/io/#read","title":"Read","text":"<pre><code>from pardox.io import read_sql\n\ndf = read_sql(\n    \"postgresql://pardox:secret@localhost:5432/mydb\",\n    \"SELECT id, amount, region FROM orders WHERE status = 'complete'\"\n)\n</code></pre>"},{"location":"guide/io/#execute-ddl-dml","title":"Execute DDL / DML","text":"<pre><code>from pardox.io import execute_sql\n\nexecute_sql(CONN, \"DROP TABLE IF EXISTS orders_archive\")\nexecute_sql(CONN, \"\"\"\n    CREATE TABLE orders_archive (\n        id      BIGINT,\n        amount  DOUBLE PRECISION,\n        region  TEXT\n    )\n\"\"\")\n# Returns 0 for DDL, affected rows for DML\nn = execute_sql(CONN, \"DELETE FROM orders WHERE status = 'cancelled'\")\n</code></pre>"},{"location":"guide/io/#write","title":"Write","text":"<pre><code># mode='append' \u2014 auto-activates COPY FROM STDIN for &gt; 10,000 rows\nrows = df.to_sql(CONN, \"orders_archive\", mode=\"append\")\nprint(f\"{rows:,} rows written\")\n\n# mode='upsert' \u2014 INSERT \u2026 ON CONFLICT (cols) DO UPDATE SET \u2026\nrows = df.to_sql(CONN, \"orders_archive\", mode=\"upsert\", conflict_cols=[\"id\"])\n</code></pre> <p>Bulk path \u2014 COPY FROM STDIN</p> <p>When writing more than 10,000 rows with <code>mode=\"append\"</code>, the Rust core automatically switches to PostgreSQL's <code>COPY FROM STDIN</code> protocol. The CSV payload is serialized in memory and streamed at wire speed \u2014 no temp files. This is typically 10x\u201350x faster than multi-row <code>INSERT</code>.</p>"},{"location":"guide/io/#5-mysql","title":"5. MySQL","text":"<p>PardoX uses the <code>mysql</code> crate v25.</p>"},{"location":"guide/io/#connection-string_1","title":"Connection string","text":"<pre><code>mysql://user:password@host:port/database\n</code></pre>"},{"location":"guide/io/#read_1","title":"Read","text":"<pre><code>from pardox.io import read_mysql\n\ndf = read_mysql(\n    \"mysql://pardox:secret@localhost:3306/mydb\",\n    \"SELECT * FROM products WHERE active = 1\"\n)\n</code></pre>"},{"location":"guide/io/#execute-ddl-dml_1","title":"Execute DDL / DML","text":"<pre><code>from pardox.io import execute_mysql\n\nexecute_mysql(CONN, \"DROP TABLE IF EXISTS products_bak\")\nexecute_mysql(CONN, \"\"\"\n    CREATE TABLE products_bak (\n        id       BIGINT,\n        name     TEXT,\n        price    DOUBLE,\n        quantity DOUBLE\n    )\n\"\"\")\n</code></pre>"},{"location":"guide/io/#write_1","title":"Write","text":"<pre><code># append \u2014 chunked INSERT 1,000 rows/stmt (auto LOAD DATA for &gt; 10k if server allows)\nrows = df.to_mysql(CONN, \"products_bak\", mode=\"append\")\n\n# replace \u2014 REPLACE INTO (delete + insert)\nrows = df.to_mysql(CONN, \"products_bak\", mode=\"replace\")\n\n# upsert \u2014 INSERT \u2026 ON DUPLICATE KEY UPDATE\nrows = df.to_mysql(CONN, \"products_bak\", mode=\"upsert\", conflict_cols=[\"id\"])\n</code></pre> <p>LOAD DATA LOCAL INFILE</p> <p>For <code>append</code> with more than 10,000 rows, PardoX attempts <code>LOAD DATA LOCAL INFILE</code>. If the server has <code>local_infile=OFF</code> (MySQL default), it falls back automatically to the 1,000-row chunked INSERT and logs a notice to stderr. To enable the fast path:</p> <pre><code>SET GLOBAL local_infile = 1;\n</code></pre>"},{"location":"guide/io/#6-sql-server","title":"6. SQL Server","text":"<p>PardoX uses the <code>tiberius</code> crate v0.12 with a single-thread Tokio runtime.</p>"},{"location":"guide/io/#connection-string-adonet-format","title":"Connection string (ADO.NET format)","text":"<pre><code>Server=host,port;Database=db;UID=user;PWD=password;TrustServerCertificate=Yes\n</code></pre>"},{"location":"guide/io/#read_2","title":"Read","text":"<pre><code>from pardox.io import read_sqlserver\n\nCONN = \"Server=localhost,1433;Database=mydb;UID=sa;PWD=MyPassword;TrustServerCertificate=Yes\"\n\ndf = read_sqlserver(CONN, \"SELECT TOP 1000 * FROM dbo.orders\")\n</code></pre>"},{"location":"guide/io/#execute-ddl-dml_2","title":"Execute DDL / DML","text":"<pre><code>from pardox.io import execute_sqlserver\n\nexecute_sqlserver(CONN, \"DROP TABLE IF EXISTS dbo.orders_bak\")\nexecute_sqlserver(CONN, \"\"\"\n    CREATE TABLE dbo.orders_bak (\n        id       BIGINT,\n        amount   FLOAT,\n        region   NVARCHAR(MAX)\n    )\n\"\"\")\n</code></pre>"},{"location":"guide/io/#write_2","title":"Write","text":"<pre><code># append \u2014 multi-row INSERT, 500 rows per statement\nrows = df.to_sqlserver(CONN, \"dbo.orders_bak\", mode=\"append\")\n\n# upsert \u2014 MERGE INTO \u2026 WHEN MATCHED \u2026 WHEN NOT MATCHED\nrows = df.to_sqlserver(CONN, \"dbo.orders_bak\", mode=\"upsert\", conflict_cols=[\"id\"])\n</code></pre> <p>Password special characters</p> <p>Avoid using <code>!</code> in SQL Server passwords. A known issue in tiberius v0.12 causes authentication failure when <code>!</code> is present in the password when connecting via TCP from an external host. Use only <code>[A-Za-z0-9_\\-@#$]</code>. A fix is tracked for v0.3.2.</p> <p>Bulk performance</p> <p>SQL Server writes use 500-row multi-value <code>INSERT</code> statements. For 50,000 rows this results in 100 round-trips instead of 50,000. A <code>BULK INSERT</code> / <code>bcp</code> path is planned for v0.3.2.</p>"},{"location":"guide/io/#7-mongodb","title":"7. MongoDB","text":"<p>PardoX uses the <code>mongodb</code> crate v2.8.</p>"},{"location":"guide/io/#connection-string-mongodb-uri","title":"Connection string (MongoDB URI)","text":"<pre><code>mongodb://user:password@host:port\n</code></pre>"},{"location":"guide/io/#read_3","title":"Read","text":"<pre><code>from pardox.io import read_mongodb\n\n# Target format: \"database.collection\"\ndf = read_mongodb(\"mongodb://admin:secret@localhost:27017\", \"mydb.orders\")\n</code></pre>"},{"location":"guide/io/#execute-commands","title":"Execute commands","text":"<pre><code>from pardox.io import execute_mongodb\n\n# Drop a collection\nexecute_mongodb(\"mongodb://...\", \"mydb\", '{\"drop\": \"orders_bak\"}')\n</code></pre>"},{"location":"guide/io/#write_3","title":"Write","text":"<pre><code># append \u2014 insert_many in batches of 10,000 documents, ordered:false\nrows = df.to_mongodb(\"mongodb://...\", \"mydb.orders_bak\", mode=\"append\")\n\n# replace \u2014 drops collection, then inserts all documents\nrows = df.to_mongodb(\"mongodb://...\", \"mydb.orders_bak\", mode=\"replace\")\n</code></pre> <p>Batch behavior</p> <p><code>ordered: false</code> is used on every <code>insert_many</code> call. This means MongoDB continues inserting valid documents even if individual documents fail (e.g., duplicate key), rather than aborting the entire batch.</p>"},{"location":"guide/io/#write-modes-summary","title":"Write Modes Summary","text":"Database <code>append</code> <code>replace</code> <code>upsert</code> PostgreSQL INSERT (COPY for &gt;10k) \u2014 INSERT ON CONFLICT DO UPDATE MySQL INSERT 1k/stmt (LOAD DATA for &gt;10k) REPLACE INTO INSERT ON DUPLICATE KEY UPDATE SQL Server Multi-row INSERT 500/stmt Multi-row INSERT 500/stmt MERGE INTO MongoDB insert_many 10k/batch drop + insert_many \u2014"},{"location":"guide/io/#table-must-exist-before-writing","title":"Table Must Exist Before Writing","text":"<p>PardoX write methods append to an existing table and do not auto-create it. Always call <code>execute_*</code> first:</p> <pre><code>from pardox.io import execute_sql\n\nexecute_sql(CONN, \"\"\"\n    CREATE TABLE IF NOT EXISTS my_table (\n        id      BIGINT,\n        amount  DOUBLE PRECISION,\n        label   TEXT\n    )\n\"\"\")\n\nrows = df.to_sql(CONN, \"my_table\", mode=\"append\")\n</code></pre>"},{"location":"guide/ml/","title":"ML Integration \u2014 Zero-Copy NumPy Bridge","text":"<p>PardoX v0.3.1 implements the NumPy <code>__array__</code> protocol, allowing direct zero-copy access from PardoX Series to NumPy arrays. No data is copied \u2014 the NumPy array points directly into the Rust HyperBlock buffer.</p>"},{"location":"guide/ml/#zero-copy-numpy-conversion","title":"Zero-Copy NumPy Conversion","text":"<pre><code>import numpy as np\nimport pardox as px\n\ndf = px.read_csv(\"features.csv\")\n\n# Direct pointer into Rust buffer \u2014 no data copy\narr = np.array(df[\"price\"])\nprint(arr.dtype)    # float64\nprint(arr.shape)    # (50000,)\nprint(arr.mean())   # identical to df[\"price\"].mean()\n</code></pre> <p>True zero-copy</p> <p><code>np.array(df['col'])</code> invokes the <code>__array__</code> dunder, which calls <code>pardox_get_f64_buffer</code> \u2014 a Rust function that returns a direct pointer into the HyperBlock's <code>Float64</code> buffer. NumPy wraps this pointer in an array without allocating new memory.</p> <p>Lifetime</p> <p>The NumPy array is valid as long as the parent DataFrame is alive. Do not hold onto <code>arr</code> after <code>df</code> goes out of scope or is freed.</p>"},{"location":"guide/ml/#scikit-learn-integration","title":"Scikit-Learn Integration","text":"<pre><code>import numpy as np\nimport pardox as px\nfrom sklearn.linear_model import LinearRegression\nfrom sklearn.preprocessing import StandardScaler\n\ndf = px.read_csv(\"training_data.csv\")\ndf.cast(\"quantity\", \"Float64\")\ndf.fillna(0.0)\n\n# Build feature matrix and target via zero-copy\nX = np.column_stack([\n    np.array(df[\"price\"]),\n    np.array(df[\"quantity\"]),\n    np.array(df[\"discount\"]),\n])\ny = np.array(df[\"tax\"])\n\n# Scale features\nscaler = StandardScaler()\nX_scaled = scaler.fit_transform(X)\n\n# Train\nmodel = LinearRegression()\nmodel.fit(X_scaled, y)\n\nprint(f\"Coefficients: {model.coef_}\")\nprint(f\"Intercept:    {model.intercept_:.4f}\")\n</code></pre>"},{"location":"guide/ml/#pytorch-integration","title":"PyTorch Integration","text":"<pre><code>import torch\nimport numpy as np\nimport pardox as px\n\ndf = px.read_csv(\"embeddings.csv\")\n\n# Zero-copy from PardoX \u2192 NumPy \u2192 PyTorch tensor\narr    = np.array(df[\"embedding_dim_0\"])\ntensor = torch.from_numpy(arr)\n\nprint(tensor.dtype)    # torch.float64\nprint(tensor.shape)    # torch.Size([50000])\n</code></pre>"},{"location":"guide/ml/#min-max-scaling-built-in","title":"Min-Max Scaling (Built-In)","text":"<p>For normalizing features before ML, PardoX provides a native Min-Max Scaler that runs entirely in Rust:</p> <pre><code># Normalize to [0, 1] \u2014 returns new DataFrame with 'result_minmax'\nnormed_df = df.min_max_scale(\"price\")\nnormed_arr = np.array(normed_df[\"result_minmax\"])\n\nprint(normed_arr.min())   # ~0.0\nprint(normed_arr.max())   # ~1.0\n</code></pre> <p>This is equivalent to <code>sklearn.preprocessing.MinMaxScaler</code> but operates at native Rust speed with no intermediate Python allocations.</p>"},{"location":"guide/ml/#supported-column-types","title":"Supported Column Types","text":"<p>The zero-copy <code>__array__</code> bridge works on <code>Float64</code> columns. For <code>Int64</code> columns, cast first:</p> <pre><code>df.cast(\"quantity\", \"Float64\")\narr = np.array(df[\"quantity\"])   # now Float64\n</code></pre> <p><code>Utf8</code> (string) columns cannot be zero-copied to NumPy. Use <code>df.to_dict()</code> or <code>df.unique()</code> to access string data.</p>"},{"location":"guide/ml/#no-numpy-import-inside-pardox","title":"No NumPy Import Inside PardoX","text":"<p>PardoX itself does not import NumPy. The <code>__array__</code> protocol is implemented entirely through <code>ctypes</code> pointer arithmetic. NumPy is only involved at the user call site when <code>np.array(series)</code> is invoked.</p> <p>This means PardoX works in environments where NumPy is not installed, and NumPy integration is opt-in.</p>"},{"location":"guide/mutation/","title":"Data Mutation &amp; Arithmetic","text":"<p>PardoX performs all arithmetic and transformations directly on Rust memory buffers using SIMD (Single Instruction, Multiple Data) instructions. No Python objects are created during computation.</p>"},{"location":"guide/mutation/#1-column-selection","title":"1. Column Selection","text":"<pre><code># Select a column \u2014 returns a Series (lightweight Rust column pointer)\nprices = df['price']\nprint(type(prices))  # &lt;class 'pardox.series.Series'&gt;\n</code></pre> <p>Zero-Copy Access</p> <p>Column selection returns a view into the Rust buffer, not a copy.</p>"},{"location":"guide/mutation/#2-vectorized-arithmetic-series-operators","title":"2. Vectorized Arithmetic (Series operators)","text":"<p>Operations via Python operators (<code>+</code>, <code>-</code>, <code>*</code>, <code>/</code>) are dispatched to SIMD-accelerated Rust kernels.</p> <pre><code># Vector \u00d7 Vector\ndf['total']      = df['price'] * df['quantity']\n\n# Vector \u00d7 Scalar (broadcast)\ndf['tax']        = df['total'] * 0.08\n\n# Chained expressions\ndf['net_amount'] = (df['total'] + df['tax']) - df['discount']\n</code></pre> <p>SIMD acceleration</p> <p>AVX2 (Intel/AMD) and NEON (Apple Silicon) process 4\u20138 values per CPU cycle \u2014 5x\u201320x faster than Python loops.</p>"},{"location":"guide/mutation/#3-dataframe-arithmetic-methods","title":"3. DataFrame Arithmetic Methods","text":"<p>These methods accept column names as strings and return a new DataFrame containing the result column.</p>"},{"location":"guide/mutation/#dfaddcol_a-col_b-addition","title":"<code>df.add(col_a, col_b)</code> \u2014 Addition","text":"<pre><code># Returns new DataFrame with column 'result_add'\nsum_df = df.add(\"price\", \"tax\")\nprint(sum_df.columns)   # ['result_add']\ntotal = sum_df['result_add'].sum()\n</code></pre>"},{"location":"guide/mutation/#dfsubcol_a-col_b-subtraction","title":"<code>df.sub(col_a, col_b)</code> \u2014 Subtraction","text":"<pre><code># Returns new DataFrame with column 'result_sub'\nprofit_df = df.sub(\"revenue\", \"cost\")\n</code></pre>"},{"location":"guide/mutation/#dfmulcol_a-col_b-multiplication","title":"<code>df.mul(col_a, col_b)</code> \u2014 Multiplication","text":"<pre><code># Returns new DataFrame with column 'result_mul'\nrevenue_df = df.mul(\"price\", \"quantity\")\nprint(f\"Revenue std dev: {revenue_df.std('result_mul'):,.2f}\")\n</code></pre>"},{"location":"guide/mutation/#dfstdcol-standard-deviation","title":"<code>df.std(col)</code> \u2014 Standard Deviation","text":"<p>Returns the sample standard deviation of a column as a scalar float. Pure Rust, no NumPy dependency.</p> <pre><code>std_val = df.std(\"amount\")\nprint(f\"Volatility: {std_val:.4f}\")\n</code></pre>"},{"location":"guide/mutation/#dfmin_max_scalecol-min-max-normalization","title":"<code>df.min_max_scale(col)</code> \u2014 Min-Max Normalization","text":"<p>Normalizes a column to the range <code>[0, 1]</code>. Returns a new DataFrame with column <code>result_minmax</code>.</p> <pre><code># Normalize prices to [0, 1]\nnormed_df = df.min_max_scale(\"price\")\nprint(normed_df['result_minmax'].min())   # ~0.0\nprint(normed_df['result_minmax'].max())   # ~1.0\n</code></pre>"},{"location":"guide/mutation/#4-type-casting","title":"4. Type Casting","text":"<p>Convert a column to a different type in-place.</p> <pre><code># Required before arithmetic when column was inferred as Int64\ndf.cast(\"quantity\", \"Float64\")\ndf.cast(\"id\",       \"Utf8\")     # convert numeric ID to string\n</code></pre> <p>Supported types: <code>Int64</code>, <code>Float64</code>, <code>Utf8</code></p>"},{"location":"guide/mutation/#5-sorting-sort_values","title":"5. Sorting (<code>sort_values</code>)","text":"<p>Sort the DataFrame by a column. Returns a new sorted DataFrame.</p> <pre><code># Ascending sort (CPU, Rust parallel merge sort)\nsorted_df = df.sort_values(\"price\", ascending=True)\n\n# Descending sort\nsorted_df = df.sort_values(\"price\", ascending=False)\n\n# GPU Bitonic sort (falls back to CPU if GPU unavailable)\nsorted_df = df.sort_values(\"price\", ascending=True, gpu=True)\n\nprint(f\"Sorted {sorted_df.shape[0]:,} rows\")\n</code></pre> <p>GPU fallback</p> <p>If a GPU is not available or the <code>wgpu</code> backend cannot initialize, PardoX automatically uses the CPU sort and logs <code>[PardoX GPU Sort] GPU not available, using CPU sort.</code> to stderr. The result is identical.</p> <p>See GPU Acceleration for details on the Bitonic sort pipeline.</p>"},{"location":"guide/mutation/#6-filtering","title":"6. Filtering","text":"<p>Apply a boolean Series as a row filter.</p> <pre><code># Single condition\nmask     = df['price'] &gt; 100.0\nfiltered = df.filter(mask)\n\n# Combined conditions\nmask2    = df['state'].eq(\"TX\")\nresult   = df.filter(mask).filter(mask2)\n</code></pre> <p>Comparison operators on Series:</p> Method Operator <code>s.eq(val)</code> <code>==</code> <code>s.neq(val)</code> <code>!=</code> <code>s.gt(val)</code> <code>&gt;</code> <code>s.gte(val)</code> <code>&gt;=</code> <code>s.lt(val)</code> <code>&lt;</code> <code>s.lte(val)</code> <code>&lt;=</code>"},{"location":"guide/mutation/#7-data-cleaning","title":"7. Data Cleaning","text":""},{"location":"guide/mutation/#fillnavalue","title":"<code>fillna(value)</code>","text":"<p>Fills <code>NaN</code> / null values in all numeric columns in-place.</p> <pre><code>df.fillna(0.0)   # replaces nulls with 0 \u2014 modifies buffer directly\n</code></pre> <p>In-place</p> <p><code>fillna</code> modifies the DataFrame in-place. No need to reassign: <code>df = df.fillna(0.0)</code>.</p>"},{"location":"guide/mutation/#rounddecimals","title":"<code>round(decimals)</code>","text":"<p>Rounds all floating-point columns to N decimal places in-place.</p> <pre><code>df.round(2)   # round all numerics to 2 decimal places\n</code></pre>"},{"location":"guide/mutation/#8-slicing-iloc","title":"8. Slicing (<code>iloc</code>)","text":"<p>Select a range of rows by position.</p> <pre><code># Rows 100 to 199 (start inclusive, end exclusive)\nsubset = df.iloc(100, 200)\nprint(subset.shape)   # (100, n_cols)\n</code></pre>"},{"location":"guide/mutation/#9-joins","title":"9. Joins","text":"<p>Hash-join two DataFrames on a key column.</p> <pre><code># Inner join on the same key column name\nresult = orders.join(customers, on=\"customer_id\")\n\n# Join on different column names\nresult = orders.join(customers, left_on=\"cust_id\", right_on=\"id\")\n</code></pre>"},{"location":"guide/mutation/#10-performance-best-practices","title":"10. Performance Best Practices","text":"<p>Do this</p> <ul> <li>Use column arithmetic (<code>df['c'] = df['a'] * df['b']</code>) for all row-level computations.</li> <li>Cast columns to <code>Float64</code> before arithmetic when they were inferred as <code>Int64</code>.</li> <li>Chain operations \u2014 Python handles precedence, Rust handles execution.</li> </ul> <p>Avoid this</p> <ul> <li>Python loops over rows (<code>for row in df</code>) destroy performance. Always use column operations.</li> </ul> <pre><code># \u274c 100x-1000x SLOWER \u2014 Python loop\nfor i in range(len(df)):\n    df['new'][i] = df['a'][i] + df['b'][i]\n\n# \u2705 FAST \u2014 SIMD vectorized\ndf['new'] = df['a'] + df['b']\n</code></pre>"},{"location":"started/","title":"Getting Started","text":"<p>Start here to install PardoX, run your first end-to-end pipeline, and check what is coming next.</p> <ul> <li>Installation</li> <li>Quick Start</li> <li>Roadmap</li> </ul>"},{"location":"started/installation/","title":"Installation","text":"<p>PardoX ships as a pre-compiled native library. No Rust compiler or C toolchain is required on the end-user machine. The Rust core (<code>libpardox.so</code> / <code>.dylib</code> / <code>.dll</code>) is bundled inside the package for each supported platform.</p>"},{"location":"started/installation/#python-sdk","title":"Python SDK","text":""},{"location":"started/installation/#via-pip","title":"via pip","text":"<pre><code>pip install pardox\n</code></pre>"},{"location":"started/installation/#system-requirements","title":"System Requirements","text":"OS Architecture Status Linux x86_64 \u2705 Stable Windows x86_64 \u2705 Stable macOS ARM64 (M1/M2/M3) \u2705 Stable macOS x86_64 (Intel) \u2705 Stable <p>Python version</p> <p>Python 3.10 or higher is required. No Rust compiler is needed \u2014 binaries are pre-packaged for all platforms.</p> <p>No database drivers needed</p> <p>PardoX connects to PostgreSQL, MySQL, SQL Server, and MongoDB entirely through the Rust core. You do not need to install <code>psycopg2</code>, <code>pymysql</code>, <code>pyodbc</code>, or <code>pymongo</code>.</p>"},{"location":"started/installation/#verify-installation","title":"Verify Installation","text":"<pre><code>import pardox as px\nprint(px.__version__)    # e.g. \"0.3.1\"\n\ndf = px.read_csv(\"my_data.csv\")\nprint(df.shape)\n</code></pre>"},{"location":"started/installation/#nodejs-sdk","title":"Node.js SDK","text":"<p>The Node.js SDK uses koffi for FFI bindings and ships the same pre-compiled <code>.so</code> / <code>.dylib</code> / <code>.dll</code>.</p> <pre><code>npm install pardox\n# or\nyarn add pardox\n</code></pre>"},{"location":"started/installation/#usage","title":"Usage","text":"<pre><code>const px = require('pardox');\n\nconst df = px.read_csv('my_data.csv');\nconsole.log(df.shape);\n</code></pre>"},{"location":"started/installation/#requirements","title":"Requirements","text":"Runtime Minimum version Node.js 18 LTS or higher"},{"location":"started/installation/#php-sdk","title":"PHP SDK","text":"<p>The PHP SDK uses the built-in <code>FFI</code> extension (PHP 7.4+).</p> <pre><code>composer require pardox/pardox-php\n</code></pre>"},{"location":"started/installation/#requirements_1","title":"Requirements","text":"Runtime Minimum version PHP 7.4 or higher FFI extension Must be enabled in <code>php.ini</code> <p>Enable the FFI extension:</p> <pre><code>; php.ini\nextension=ffi\nffi.enable=true\n</code></pre>"},{"location":"started/installation/#usage_1","title":"Usage","text":"<pre><code>&lt;?php\nrequire_once 'vendor/autoload.php';\n\nuse PardoX\\DataFrame;\n\n$df = DataFrame::read_csv('my_data.csv');\necho $df-&gt;shape[0] . \" rows\\n\";\n</code></pre>"},{"location":"started/installation/#docker-databases-for-testing","title":"Docker (Databases for Testing)","text":"<p>A <code>docker-compose.yml</code> is provided to spin up all supported databases locally:</p> <pre><code>docker-compose up -d\n</code></pre> <p>This starts:</p> Container Database Host port <code>pardox_validation_db</code> PostgreSQL 16 5434 <code>pardox_mysql</code> MySQL 8.0 3306 <code>pardox_sqlserver</code> SQL Server 2022 1433 <p>MongoDB</p> <p>Add a MongoDB service to the compose file with <code>mongo:7.0</code> if needed for your environment.</p>"},{"location":"started/quickstart/","title":"Quick Start","text":"<p>This guide walks through a complete v0.3.1 data pipeline: loading a CSV, running vectorized math, inspecting results, and writing to a relational database \u2014 all in native Rust speed.</p>"},{"location":"started/quickstart/#hello-world","title":"Hello World","text":"<pre><code>import pardox as px\n\n# 1. Load data \u2014 parallel Rust CSV parser, automatic type inference\ndf = px.read_csv(\"sales_data.csv\")\nprint(f\"Loaded {df.shape[0]:,} rows \u00d7 {df.shape[1]} columns\")\n\n# 2. Inspect columns\nprint(df.columns)\ndf.show(5)   # ASCII table preview\n\n# 3. Cast and compute\ndf.cast(\"quantity\", \"Float64\")\nrevenue_df = df.mul(\"price\", \"quantity\")   # new DataFrame with 'result_mul'\n\n# 4. Statistics\nstd_val = revenue_df.std(\"result_mul\")\nprint(f\"Revenue std dev: {std_val:,.2f}\")\n\n# 5. Write to PostgreSQL (COPY FROM STDIN auto-activated for &gt; 10k rows)\nfrom pardox.io import execute_sql\nCONN = \"postgresql://user:password@localhost:5432/mydb\"\nexecute_sql(CONN, \"CREATE TABLE IF NOT EXISTS sales (price FLOAT, quantity FLOAT)\")\nrows = df.to_sql(CONN, \"sales\", mode=\"append\")\nprint(f\"Written {rows:,} rows to PostgreSQL\")\n</code></pre>"},{"location":"started/quickstart/#step-by-step-breakdown","title":"Step-by-Step Breakdown","text":""},{"location":"started/quickstart/#1-ingestion-read_csv","title":"1. Ingestion (<code>read_csv</code>)","text":"<p>PardoX spawns a Rust thread pool to parse chunks of the CSV file in parallel. No Python objects are created during ingestion \u2014 data flows directly into Rust-managed memory buffers.</p> <pre><code>df = px.read_csv(\"dataset.csv\")\n</code></pre> <p>Type inference</p> <p>The engine scans the first rows to classify each column as <code>Int64</code>, <code>Float64</code>, or <code>Utf8</code> (string). You can override with an explicit schema:</p> <pre><code>df = px.read_csv(\"data.csv\", schema={\"price\": \"Float64\", \"id\": \"Int64\"})\n</code></pre>"},{"location":"started/quickstart/#2-inspect-show-shape-columns-dtypes","title":"2. Inspect (<code>show</code>, <code>shape</code>, <code>columns</code>, <code>dtypes</code>)","text":"<pre><code>print(df.shape)      # (50000, 14)\nprint(df.columns)    # ['id', 'price', 'quantity', ...]\nprint(df.dtypes)     # {'id': 'Utf8', 'price': 'Float64', ...}\ndf.show(5)           # ASCII table, first 5 rows\n</code></pre>"},{"location":"started/quickstart/#3-type-casting-cast","title":"3. Type Casting (<code>cast</code>)","text":"<p>When a column is inferred as <code>Int64</code> but you need <code>Float64</code> for arithmetic:</p> <pre><code>df.cast(\"quantity\", \"Float64\")\n</code></pre> <p>Supported types: <code>Int64</code>, <code>Float64</code>, <code>Utf8</code>.</p>"},{"location":"started/quickstart/#4-vectorized-arithmetic","title":"4. Vectorized Arithmetic","text":"<pre><code># Series operators (via Proxy / __getitem__)\ndf['total'] = df['price'] * df['quantity']\ndf['tax']   = df['total'] * 0.16\n\n# DataFrame-level methods (return new DataFrame)\nrevenue_df    = df.mul(\"price\", \"quantity\")   # result column: 'result_mul'\nprofit_df     = df.sub(\"revenue\", \"cost\")     # result column: 'result_sub'\n</code></pre>"},{"location":"started/quickstart/#5-native-math-methods","title":"5. Native Math Methods","text":"<pre><code># Standard deviation (pure Rust, no NumPy)\nstd_val = revenue_df.std(\"result_mul\")\n\n# Min-Max normalization \u2192 new DataFrame with 'result_minmax' column\nnormed_df = df.min_max_scale(\"price\")\n\n# Sort by column (CPU or GPU)\nsorted_df = df.sort_values(\"price\", ascending=False)\nsorted_df = df.sort_values(\"price\", ascending=True, gpu=True)  # GPU Bitonic sort\n</code></pre>"},{"location":"started/quickstart/#6-observer-data-inspection-export","title":"6. Observer \u2014 Data Inspection &amp; Export","text":"<pre><code># Value frequency table\nstate_counts = df.value_counts(\"state\")\nprint(state_counts)   # {'TX': 6345, 'CA': 6301, ...}\n\n# Unique values in a column\nunique_cats = df.unique(\"category\")\n\n# Export full DataFrame to Python\nrecords = df.to_dict()   # list of dicts \u2014 50k records\njson_str = df.to_json()  # JSON string \"[{...}, ...]\"\n</code></pre>"},{"location":"started/quickstart/#7-database-write-relational-conqueror","title":"7. Database Write (Relational Conqueror)","text":"<pre><code>from pardox.io import execute_sql, execute_mysql, read_sql\n\nPG_CONN    = \"postgresql://pardox:secret@localhost:5432/mydb\"\nMYSQL_CONN = \"mysql://pardox:secret@localhost:3306/mydb\"\n\n# PostgreSQL \u2014 auto COPY FROM STDIN for &gt; 10k rows\nexecute_sql(PG_CONN, \"CREATE TABLE IF NOT EXISTS sales (price FLOAT, quantity FLOAT)\")\nrows = df.to_sql(PG_CONN, \"sales\", mode=\"append\")\nprint(f\"Postgres: {rows:,} rows written\")\n\n# MySQL \u2014 chunked batch INSERT (auto LOAD DATA if server allows)\nexecute_mysql(MYSQL_CONN, \"CREATE TABLE IF NOT EXISTS sales (price DOUBLE, quantity DOUBLE)\")\nrows = df.to_mysql(MYSQL_CONN, \"sales\", mode=\"append\")\nprint(f\"MySQL: {rows:,} rows written\")\n\n# Read back from PostgreSQL\ndf_check = read_sql(PG_CONN, \"SELECT COUNT(*) FROM sales\")\n</code></pre>"},{"location":"started/quickstart/#8-zero-copy-numpy-integration","title":"8. Zero-Copy NumPy Integration","text":"<pre><code>import numpy as np\n\n# Direct pointer from Rust buffer \u2014 no data copy\narr = np.array(df[\"price\"])\nprint(arr.dtype)   # float64\nprint(arr.mean())  # same value as df[\"price\"].mean()\n</code></pre>"},{"location":"started/quickstart/#9-persist-to-prdx","title":"9. Persist to .prdx","text":"<pre><code># Save\ndf.to_prdx(\"sales_processed.prdx\")\n\n# Load (4.6 GB/s read throughput)\ndf2 = px.read_prdx(\"sales_processed.prdx\")\n</code></pre> <p>Performance benchmark</p> <p>Loading 2 GB of data: CSV ~8s \u00b7 Parquet ~3s \u00b7 PRDX ~0.5s</p>"},{"location":"started/quickstart/#full-pipeline-example","title":"Full Pipeline Example","text":"<pre><code>import pardox as px\nfrom pardox.io import execute_sql\n\nCONN = \"postgresql://pardox:secret@localhost:5432/analytics\"\n\n# Load\ndf = px.read_csv(\"sales_50k.csv\")\ndf.cast(\"quantity\", \"Float64\")\n\n# Transform\ndf.fillna(0.0)\ndf['revenue'] = df['price'] * df['quantity']\ndf['tax']     = df['revenue'] * 0.08\n\n# Analyze\nprint(f\"Total revenue : ${df['revenue'].sum():,.2f}\")\nprint(f\"Avg ticket    : ${df['revenue'].mean():,.2f}\")\nprint(f\"Std deviation : {df['revenue'].std():,.2f}\")\n\n# Inspect\ntop_states = df.value_counts(\"state\")\nprint(\"Top states:\", list(top_states.items())[:5])\n\n# Write\nexecute_sql(CONN, \"DROP TABLE IF EXISTS sales_results\")\nexecute_sql(CONN, (\n    \"CREATE TABLE sales_results \"\n    \"(price FLOAT, quantity FLOAT, revenue FLOAT, tax FLOAT)\"\n))\nrows = df.to_sql(CONN, \"sales_results\", mode=\"append\")\nprint(f\"\\nWrote {rows:,} rows to PostgreSQL\")\n\n# Save locally\ndf.to_prdx(\"sales_results.prdx\")\nprint(\"Saved to sales_results.prdx\")\n</code></pre>"},{"location":"started/roadmap/","title":"Roadmap","text":"<p>This page tracks features planned for upcoming releases and the reasoning behind them.</p>"},{"location":"started/roadmap/#v031-released-2026-02-28","title":"v0.3.1 \u2014 Released (2026-02-28)","text":"<p>All items below were delivered in this release.</p> Feature Status PostgreSQL read / write / execute \u2705 Done MySQL read / write / execute \u2705 Done SQL Server read / write / execute \u2705 Done MongoDB read / write / execute \u2705 Done PostgreSQL COPY FROM STDIN (&gt; 10k rows) \u2705 Done MySQL LOAD DATA LOCAL INFILE (&gt; 10k rows) \u2705 Done (server-side flag required) SQL Server batch INSERT 500 rows/stmt \u2705 Done SQL Server MERGE upsert \u2705 Done MongoDB ordered:false, 10k doc batches \u2705 Done <code>to_dict()</code>, <code>to_json()</code> \u2014 full DataFrame export \u2705 Done <code>value_counts()</code>, <code>unique()</code> \u2014 EDA Observer \u2705 Done <code>df.add()</code>, <code>df.sub()</code>, <code>df.std()</code>, <code>df.min_max_scale()</code> \u2705 Done <code>df.sort_values(ascending, gpu)</code> \u2705 Done GPU Bitonic sort (WebGPU / wgpu) with CPU fallback \u2705 Done Zero-copy NumPy bridge (<code>__array__</code> protocol) \u2705 Done PHP SDK \u2014 full feature parity \u2705 Done Node.js SDK \u2014 full feature parity \u2705 Done FFI memory safety \u2014 heap vs. thread-local string ownership \u2705 Fixed"},{"location":"started/roadmap/#v032-planned","title":"v0.3.2 \u2014 Planned","text":""},{"location":"started/roadmap/#sql-server-password-special-character-fix","title":"SQL Server \u2014 Password Special Character Fix","text":"<p>Problem: The tiberius Rust driver (v0.12) fails to authenticate when the SQL Server password contains <code>!</code> or certain other special characters. The connection succeeds from the local machine (loopback), but fails from an external host. Root cause is in the <code>unicode_password()</code> XOR encoding function inside the TDS <code>LOGIN7</code> packet.</p> <p>Planned fix: Audit <code>tiberius</code> v0.12.x <code>login.rs</code> \u2014 specifically the <code>unicode_password()</code> function \u2014 and patch or upgrade to a version where the encoding is correct for all printable ASCII characters.</p> <p>Workaround until fixed: Use a SQL Server password that contains only <code>[A-Za-z0-9_\\-@#$]</code>.</p>"},{"location":"started/roadmap/#error-code-hierarchy","title":"Error Code Hierarchy","text":"<p>Problem: All database errors currently return <code>-100</code> regardless of the actual failure reason, making diagnostics difficult.</p> <p>Planned improvement:</p> Code range Category -100 to -199 Connection / authentication errors -200 to -299 Query parse / syntax errors -300 to -399 Data type mismatch -400 to -499 Constraint violations -500 to -599 Timeout errors"},{"location":"started/roadmap/#additional-native-data-sources","title":"Additional Native Data Sources","text":"<p>The following connectors are planned for native Rust implementation (no Python driver dependency):</p> Source Format / Protocol Notes Apache Parquet Binary columnar Direct read into HyperBlock via <code>parquet2</code> crate Apache Kafka Streaming Topic \u2192 DataFrame streaming ingestion AWS S3 Object storage CSV / Parquet over S3 via <code>aws-sdk-rust</code> Snowflake JDBC-like Via Snowflake Arrow export Oracle DB OCI Via <code>oracle</code> Rust crate SQLite File-based SQL Via <code>rusqlite</code>"},{"location":"started/roadmap/#fake-postgresql-server-validation-tool","title":"Fake PostgreSQL Server (Validation Tool)","text":"<p>Purpose: Enable SDK validation tests to run without a real PostgreSQL instance. A lightweight Rust server implementing a subset of the PostgreSQL wire protocol will respond to <code>SELECT</code>, <code>INSERT</code>, <code>COPY</code>, and DDL statements, returning predictable results.</p> <p>Use cases: - CI/CD pipeline tests that cannot provision real databases - SDK integration tests against a controlled server - Protocol-level regression testing for the <code>pardox_write_sql</code> COPY path</p>"},{"location":"started/roadmap/#groupby-split-apply-combine","title":"GroupBy / Split-Apply-Combine","text":"<p>Vectorized <code>groupby</code> with Rust hash-aggregation engine.</p> <pre><code># Coming in v0.3.2\nsummary = df.groupby(\"region\").agg({\"revenue\": \"sum\", \"quantity\": \"mean\"})\n</code></pre>"},{"location":"started/roadmap/#dataframe-filtering-dsl","title":"DataFrame Filtering DSL","text":"<p>A fluent filter API backed by Rust predicate evaluation.</p> <pre><code># Coming in v0.3.2\ndf_filtered = df.filter(df[\"price\"] &gt; 100).filter(df[\"state\"] == \"TX\")\n</code></pre>"},{"location":"started/roadmap/#long-term-vision","title":"Long-Term Vision","text":"<ul> <li>Distributed execution: Partition HyperBlocks across nodes via a Rust-native scheduler.</li> <li>Streaming DataFrames: Ingest from Kafka / Kinesis as append-only HyperBlock partitions.</li> <li>GPU-accelerated aggregations: Extend GPU support from sort to sum, mean, and groupby using compute shaders.</li> <li>WASM SDK: PardoX in the browser via WebAssembly.</li> </ul>"}]}